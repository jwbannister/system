options(repos = c(CRAN = "https://cran.rstudio.com"))
.libPaths("~/lib/R")
#library(devtools)
#library(magrittr)
#library(colorout)

set.seed(42)
options(width = 80)
options(digits = 10)
options(stringsAsFactors=FALSE)
options(max.print = 80)

source("~/system/R/rprofile_functions.R")
# run script to open help files in separate xterm window
xthelp()

# compare installed packages against master list and install missing
pkgs <- check_packages()

