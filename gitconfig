[user]
	name = John Bannister
	email = jbannister@airsci.com
[core]
	editor = vim
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
[remote "origin"]
	receivepack = git receive-pack
